# List of docker's aliases
alias dcb="dc build"
alias dcd="dc down"
alias dce="dc exec"
alias dcr="dc run"
alias dcu="dc up"
alias vim="nvim"

if [ -f "$HOME/.aliases.local" ] ; then
    source "$HOME/.aliases.local"
fi
